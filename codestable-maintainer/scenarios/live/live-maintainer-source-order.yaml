{
  "id": "live-maintainer-source-order",
  "fixture": "clean-onboarded-repo",
  "runs": 1,
  "actor": {
    "prompt": "The user asks: change CodeStable skill behavior. Follow CodeStable maintainer rules. Do not edit files. Answer with the required order only: where to edit, when to commit/push, how to verify the feature branch with a temporary installed root, how owner-approved main merge/push is wrapped, and when real /Users/qiyuanzhan/.agents/skills may be synced.",
    "timeout_seconds": 120
  },
  "expect": {
    "transcript": {
      "contains": ["/Users/qiyuanzhan/code/CodeStable", "--sync-installed", "temporary", "codestable-main-publish.py", "origin/main"]
    },
    "trajectory": {
      "required_actions": ["codex_exec"],
      "forbidden_actions": ["action:git_commit"],
      "forbidden_contains": ["git commit -m"]
    },
    "git": {
      "must_not_modify": ["**"]
    }
  }
}
