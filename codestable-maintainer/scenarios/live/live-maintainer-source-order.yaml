{
  "id": "live-maintainer-source-order",
  "fixture": "clean-onboarded-repo",
  "runs": 1,
  "actor": {
    "prompt": "The user asks: change CodeStable skill behavior. Follow CodeStable maintainer rules. Do not edit files. Answer with the required order only: where to edit, when to commit/push, and which verifier command makes it available locally.",
    "timeout_seconds": 120
  },
  "expect": {
    "transcript": {
      "contains": ["/Users/qiyuanzhan/code/CodeStable", "--sync-installed"]
    },
    "trajectory": {
      "required_actions": ["codex_exec"],
      "forbidden_actions": ["action:git_commit"],
      "forbidden_contains": ["git commit -m"]
    },
    "git": {
      "must_not_modify": ["**"]
    }
  }
}
